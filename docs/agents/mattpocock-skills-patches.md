# Matt Pocock skills — local patch ledger

Every local change applied to a vendored skill, one row per patch. Each change is marked in the vendored file
itself with `<!-- bukerov-local-patch: <id> -->` / `<!-- /bukerov-local-patch: <id> -->` comments around the
touched text. All unpatched content is byte-identical to upstream commit `ed37663cc5fbef691ddfecd080dff42f7e7e350d`
(see `mattpocock-skills-manifest.json` for per-skill `upstream_blob_sha`). No translations, no stylistic
rewrites — every change here is a capability/safety constraint, not a wording preference.

| Skill | Upstream path | Blob SHA (SKILL.md) | Local change | Reason | Rule |
| --- | --- | --- | --- | --- | --- |
| implement | `skills/engineering/implement/SKILL.md` | `7a0b11f5f4fe9505ea5c7983c3083ba1bf754f69` | Replaced the unconditional "Commit your work to the current branch." with a capability-aware envelope (commit only if the contract allows it and only on the task branch, never a protected branch; non-force push only under PUBLISH_DRAFT; Draft PR only when authorized; Ready/merge/release/deploy owner-gated per GOVERNANCE_V3.md §4). Envelope wording re-anchored to the canonical two-class model (owner-gated vs always-forbidden) at the Governance v3 adoption (ADR-0002). | Upstream assumed standing commit/push authority; this project's default is READ_ONLY. | `implement-commit-envelope` |
| resolving-merge-conflicts | `skills/engineering/resolving-merge-conflicts/SKILL.md` | `aadb3fcb1dfb43413dff30c2fb4b18b7cf58e90e` | Body: "Always resolve; never `--abort`" softened to allow proposing `--abort`; final step changed from "stage everything and commit... continue the rebase" to "present the conflict map and resolved diff, then STOP — no `git add -A`, no commit/`merge --continue`/`rebase --continue`/push without a separate explicit envelope." | Conflict resolution is high-risk; this project treats it as analysis-and-recommend, not autonomous commit. | `merge-conflicts-read-only` |
| resolving-merge-conflicts (frontmatter) | `skills/engineering/resolving-merge-conflicts/SKILL.md` | `aadb3fcb1dfb43413dff30c2fb4b18b7cf58e90e` | Added `disable-model-invocation: true` to the frontmatter, moving invocation from model-triggered to user-invoked only. | A merge-conflict skill this consequential should never trigger implicitly from model judgment. | `merge-conflicts-read-only` |
| prototype | `skills/engineering/prototype/SKILL.md` | `e75d5331ceffd9b2c5a9554c3db124d848afa054` | "Commit it to a throwaway branch, out of main" changed to a context pointer only; added that prototypes live in `/tmp` or a disposable worktree, never the primary checkout, with no branch/commit/push without an explicit contract. | Prevents throwaway exploratory code from landing on the primary checkout or requiring branch/commit authority it wasn't granted. | `prototype-disposable` |
| prototype (UI.md) | `skills/engineering/prototype/UI.md` | `76c0f6012b016af04d6105fa696a9a0e29dfa53a`* | Added a pointer block: every shared-component/styling-system/branch edit this reference file describes is still subject to `SKILL.md`'s `/tmp`-or-disposable-worktree rule and the no-branch/commit/push-without-contract rule. | UI.md's own text ("fold the winner into the real code", "throwaway branch") could be read in isolation as standing authority to branch/commit; the pointer closes that reading. | `prototype-disposable` |
| prototype (LOGIC.md) | `skills/engineering/prototype/LOGIC.md` | `fe9a2c29f77b9b7182ad7fa4bd251f27e506b7d9`* | Same pointer block as UI.md, adapted to LOGIC.md's "lift into the real module" / "throwaway branch" language. | Same rationale as UI.md — the sibling reference file needed the same explicit tie-back to `SKILL.md`'s rule. | `prototype-disposable` |
| research | `skills/engineering/research/SKILL.md` | `0ba594a07f306479baa67104381f48e209ab6aae` | Added default READ_ONLY note (session-scoped background agent, untrusted-web-data caveat); output path gated on the `write_research_docs` capability instead of writing into the repo unconditionally. | Aligns research output location with the operation-modes/task-contract model instead of assuming repo write access. | `research-read-only` |
| tdd | `skills/engineering/tdd/SKILL.md` | `9a2e1d2a1ad856b0d5903dd002209ff8c32c9a48` | Added a note that a seam counts as pre-agreed (no re-ask) when it's already defined by the task contract, an approved spec, an ADR, or the Wave-1 design manifest. | Avoids redundant seam-confirmation prompts when the seam was already settled upstream of the TDD loop. | `tdd-seam-preagreed` |
| code-review | `skills/engineering/code-review/SKILL.md` | `2a0b5240731b927caa9ac0bf43c3e2af9dc3f0a7` | Added an explicit always-read-only statement (no edits, no GitHub review publication); changed "use the `general-purpose` subagent for both" to "use the read-only `Explore` subagent"; noted subagent count is capped by the task contract. | Code review must never silently gain write/publish side effects, and should use the most restricted subagent capable of the job. | `code-review-read-only` |
| triage | `skills/engineering/triage/SKILL.md` | `be47b78100c8524c3af3681790d2bb8b9ff150b7` | "Quick state override" step changed from "confirm what you're about to do, then act" to: tracker is read-only by default, propose classification/labels/comments/close, apply only under an explicit task contract. | Prevents label/comment/close mutations from happening on a maintainer's shorthand instruction without a contract. | `triage-read-only-default` |
| triage (step 5) | `skills/engineering/triage/SKILL.md` | `be47b78100c8524c3af3681790d2bb8b9ff150b7` | Duplicated the read-only-by-default gate into step 5 "Apply the outcome" (posting a brief/comment, applying a label, closing an issue) — not just the "Quick state override" shortcut path. | The original patch only covered the maintainer-shorthand path; the main triage walkthrough's own "Apply the outcome" step reached the tracker through a separate code path that needed the same gate. | `triage-read-only-default` |
| to-spec | `skills/engineering/to-spec/SKILL.md` | `f3cca8d3dbdb7c22e11447f1d13011a78ca6efba` | "Publish it to the project issue tracker. Apply the `ready-for-agent` triage label" changed to: produce a local draft first; publish to the tracker only under an explicit contract. | Spec drafts shouldn't require tracker-write authority to produce. | `to-spec-local-first` |
| to-spec (frontmatter) | `skills/engineering/to-spec/SKILL.md` | `f3cca8d3dbdb7c22e11447f1d13011a78ca6efba` | `description` field reworded from "publish it to the project issue tracker" to "produced as a local draft first — publish ... only under an explicit task contract". | The skill's own frontmatter summary (visible before the body loads) still described the old unconditional-publish behavior. | `to-spec-local-first` |
| to-tickets | `skills/engineering/to-tickets/SKILL.md` | `96deac51d4391a3f691478d48f85f43261516c08` | Publish step changed to default local ticket set + dependency graph; real-tracker publication (never dozens automatically) gated on an explicit contract. | Same rationale as to-spec — ticket drafting shouldn't require tracker-write authority. | `to-tickets-local-first` |
| to-tickets (frontmatter) | `skills/engineering/to-tickets/SKILL.md` | `96deac51d4391a3f691478d48f85f43261516c08` | `description` field reworded from "published to the configured tracker" to "written as a local ticket set by default — publishing ... only under an explicit task contract". | Same rationale as to-spec's frontmatter fix. | `to-tickets-local-first` |
| wayfinder | `skills/engineering/wayfinder/SKILL.md` | `42e3644cc57e41a1b87482754f25b4d9462d4bbc` | Map/tickets default to local files (`.scratch/` or `/tmp`); any tracker mutation requires an explicit contract; research subagents scoped to the active session and its agent cap; no `research/<name>` branch push without an authorizing envelope. | Wayfinder's map/ticket creation and branch pushes are tracker/VCS mutations that need the same gating as everything else. | `wayfinder-local-default` |
| improve-codebase-architecture | `skills/engineering/improve-codebase-architecture/SKILL.md` | `b56969e92f0705d70700f908b8ec929a1edfa782` | Report generation changed from Tailwind/Mermaid CDN imports to fully self-contained HTML (inline CSS, inline SVG/ASCII diagrams); auto-open (`xdg-open`/`open`/`start`) removed in favor of printing the path. Same two changes mirrored in `HTML-REPORT.md`'s scaffold and diagram-pattern sections. | A CDN-loading HTML report is a network dependency and a mild supply-chain surface; auto-opening a file is an unrequested side effect. | `architecture-selfcontained-html`, `architecture-no-autoopen` |
| handoff | `skills/productivity/handoff/SKILL.md` | `043d9e13dc7eca3002a47d3ab9865c568f647863` | Save location changed from the OS temp directory to a gitignored `.scratch/` directory or the session scratchpad; added exact repo/branch/HEAD SHA + contract state + next step; strengthened redaction wording (API keys, OAuth tokens, cookies, passwords -> `[REDACTED]`, omit if unsure). | A world-readable OS temp handoff file is a weaker boundary than a gitignored in-workspace location; handoffs need enough state to resume safely without re-deriving it. | `handoff-scratch-redact` |
| teach | `skills/productivity/teach/SKILL.md` | `b1603e5ac536d2b5c29496c06df4db0bb9f74e69` | Teaching workspace changed from "the current directory" to a dedicated learning directory the user confirms first, never the production source repo root; removed auto-opening lesson files in favor of printing the path. | Prevents a teaching session from treating this Go repo's working tree as scratch space for lesson HTML/asset files. | `teach-workspace` |
| diagnosing-bugs | `skills/engineering/diagnosing-bugs/SKILL.md` | `f400de7c1937377fec7ff9bae3b0c072670f1e81` | Post-mortem checklist item "stated in the commit / PR message" changed to "recorded in the diagnosis summary and, when the task contract authorizes a commit, in the commit/PR message too." | The finding must be captured even when no commit is authorized for the session. | `diagnosing-bugs-commit-note` |
| ask-matt | `skills/engineering/ask-matt/SKILL.md` | `70b807b35f85859b0c6cced1919f56e024e6a753` | "Precondition" section's pointer to `/setup-matt-pocock-skills` replaced with a pointer to this project's already-integrated config (`docs/agents/issue-tracker.md`, `domain.md`, `triage-labels.md`); explicit note not to suggest running the excluded setup skill. | `setup-matt-pocock-skills` is excluded from this project's vendored set (see manifest); ask-matt must not route users to a skill that isn't installed. | `ask-matt-setup-pointer` |
| codebase-design | `skills/engineering/codebase-design/DESIGN-IT-TWICE.md` | `49a7c42a2ccc6aff0ffc09efd28e6a4aa3c373d7`* | "Spawn 3+ sub-agents in parallel" changed to respect the active task contract's `agent_cap`, reducing the count below 3 if the cap requires it. | Prevents Design-It-Twice from exceeding the session's agent budget set by the task contract. | `design-it-twice-cap` |

\* `DESIGN-IT-TWICE.md`, `UI.md`, and `LOGIC.md` are sibling reference files, not `SKILL.md`. Their blob SHA
column records each file's *own* upstream blob SHA (per `scratchpad/upstream-blob-shas.txt` at review time),
which differs from the skill's `SKILL.md` SHA recorded in `mattpocock-skills-manifest.json` — that manifest
only tracks the `SKILL.md` blob per skill; this ledger is the place a sibling file's own SHA is recorded. See
`docs/agents/mattpocock-skills-manifest.json` for the full file set vendored per skill.
