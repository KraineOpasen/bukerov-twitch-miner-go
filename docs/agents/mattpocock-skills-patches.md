# Matt Pocock skills — local patch ledger

Every local change applied to a vendored skill, one row per patch. Each change is marked in the vendored file
itself with `<!-- bukerov-local-patch: <id> -->` / `<!-- /bukerov-local-patch: <id> -->` comments around the
touched text. All unpatched content is byte-identical to upstream commit `068b6e0c62393147daf03530149cdce209c93da8`
(refreshed from `ed37663cc5fbef691ddfecd080dff42f7e7e350d`; see `mattpocock-skills-manifest.json` for per-skill
`upstream_blob_sha`/`vendored_blob_sha`). No translations, no stylistic rewrites — every change here is a
capability/safety constraint, not a wording preference. Where an upstream edit landed inside a patched hunk, the
patch was reapplied via a 3-way merge (old-upstream base, previously-vendored ours, new-upstream theirs) so
upstream's new wording survives alongside the constraint; those rows note it below.

| Skill | Upstream path | Blob SHA (SKILL.md) | Local change | Reason | Rule |
| --- | --- | --- | --- | --- | --- |
| implement | `skills/engineering/implement/SKILL.md` | `7a0b11f5f4fe9505ea5c7983c3083ba1bf754f69` | Replaced the unconditional "Commit your work to the current branch." with a capability-aware envelope (commit only if the contract allows it and only on the task branch; push only under PUBLISH_DRAFT; Draft PR only when authorized; never Ready/merge/release/deploy). | Upstream assumed standing commit/push authority; this project's default is READ_ONLY. | `implement-commit-envelope` |
| resolving-merge-conflicts | `skills/engineering/resolving-merge-conflicts/SKILL.md` | `aadb3fcb1dfb43413dff30c2fb4b18b7cf58e90e` | Body: "Always resolve; never `--abort`" softened to allow proposing `--abort`; final step changed from "stage everything and commit... continue the rebase" to "present the conflict map and resolved diff, then STOP — no `git add -A`, no commit/`merge --continue`/`rebase --continue`/push without a separate explicit envelope." | Conflict resolution is high-risk; this project treats it as analysis-and-recommend, not autonomous commit. | `merge-conflicts-read-only` |
| resolving-merge-conflicts (frontmatter) | `skills/engineering/resolving-merge-conflicts/SKILL.md` | `aadb3fcb1dfb43413dff30c2fb4b18b7cf58e90e` | Added `disable-model-invocation: true` to the frontmatter, moving invocation from model-triggered to user-invoked only. | A merge-conflict skill this consequential should never trigger implicitly from model judgment. | `merge-conflicts-read-only` |
| prototype | `skills/engineering/prototype/SKILL.md` | `094571156140f5993cce8557dc31383c82817f3e` | "Commit it to a throwaway branch, out of main" changed to a context pointer only; added that prototypes live in `/tmp` or a disposable worktree, never the primary checkout, with no branch/commit/push without an explicit contract. | Prevents throwaway exploratory code from landing on the primary checkout or requiring branch/commit authority it wasn't granted. | `prototype-disposable` |
| prototype (UI.md) | `skills/engineering/prototype/UI.md` | `76c0f6012b016af04d6105fa696a9a0e29dfa53a`* | Added a pointer block: every shared-component/styling-system/branch edit this reference file describes is still subject to `SKILL.md`'s `/tmp`-or-disposable-worktree rule and the no-branch/commit/push-without-contract rule. | UI.md's own text ("fold the winner into the real code", "throwaway branch") could be read in isolation as standing authority to branch/commit; the pointer closes that reading. | `prototype-disposable` |
| prototype (LOGIC.md) | `skills/engineering/prototype/LOGIC.md` | `5f5a3fd5a8cbd69c029854e9881ddc6e87ae5093`* | Same pointer block as UI.md, adapted to LOGIC.md's "lift into the real module" / "throwaway branch" language. Reapplied via 3-way merge in the 068b6e0c refresh; upstream's own edits to LOGIC.md elsewhere in the file survived unpatched. | Same rationale as UI.md — the sibling reference file needed the same explicit tie-back to `SKILL.md`'s rule. | `prototype-disposable` |
| research | `skills/engineering/research/SKILL.md` | `0ba594a07f306479baa67104381f48e209ab6aae` | Added default READ_ONLY note (session-scoped background agent, untrusted-web-data caveat); output path gated on the `write_research_docs` capability instead of writing into the repo unconditionally. | Aligns research output location with the operation-modes/task-contract model instead of assuming repo write access. | `research-read-only` |
| tdd | `skills/engineering/tdd/SKILL.md` | `d8a646611ed9d2a9d2e5ea1f9b9b82d4f0821774` | Added a note that a seam counts as pre-agreed (no re-ask) when it's already defined by the task contract, an approved spec, an ADR, or the Wave-1 design manifest. Reapplied via 3-way merge in the 068b6e0c refresh; upstream added an adjacent new paragraph pointing to `codebase-design` when the interface shape itself is in question, which now follows our patch note unchanged. | Avoids redundant seam-confirmation prompts when the seam was already settled upstream of the TDD loop. | `tdd-seam-preagreed` |
| code-review | `skills/engineering/code-review/SKILL.md` | `8ff164acd5f343ed02f361ff796732c31c72f878` | Added an explicit always-read-only statement (no edits, no GitHub review publication); changed "use the `general-purpose` subagent for both" to "use the read-only `Explore` subagent"; noted subagent count is capped by the task contract. Reapplied via 3-way merge in the 068b6e0c refresh: upstream reworded the adjacent issue-tracker sentence and dropped its own "send a single message with two `Agent` tool calls" line, so the parallel-invocation detail now lives inside our patch note instead. | Code review must never silently gain write/publish side effects, and should use the most restricted subagent capable of the job. | `code-review-read-only` |
| triage | `skills/engineering/triage/SKILL.md` | `41749a9c458b91c3bf41aef480e93d229b316853` | "Quick state override" step changed from "confirm what you're about to do, then act" to: tracker is read-only by default, propose classification/labels/comments/close, apply only under an explicit task contract. | Prevents label/comment/close mutations from happening on a maintainer's shorthand instruction without a contract. | `triage-read-only-default` |
| triage (step 5) | `skills/engineering/triage/SKILL.md` | `41749a9c458b91c3bf41aef480e93d229b316853` | Duplicated the read-only-by-default gate into step 5 "Apply the outcome" (posting a brief/comment, applying a label, closing an issue) — not just the "Quick state override" shortcut path. | The original patch only covered the maintainer-shorthand path; the main triage walkthrough's own "Apply the outcome" step reached the tracker through a separate code path that needed the same gate. | `triage-read-only-default` |
| to-spec | `skills/engineering/to-spec/SKILL.md` | `54dbc4bfea178525d64ac8e7b95791326aff4659` | "Publish it to the project issue tracker. Apply the `ready-for-agent` triage label" changed to: produce a local draft first; publish to the tracker only under an explicit contract. | Spec drafts shouldn't require tracker-write authority to produce. | `to-spec-local-first` |
| to-spec (frontmatter) | `skills/engineering/to-spec/SKILL.md` | `54dbc4bfea178525d64ac8e7b95791326aff4659` | `description` field reworded from "publish it to the project issue tracker" to "produced as a local draft first — publish ... only under an explicit task contract". | The skill's own frontmatter summary (visible before the body loads) still described the old unconditional-publish behavior. | `to-spec-local-first` |
| to-tickets | `skills/engineering/to-tickets/SKILL.md` | `4a21285cbd01c7d2c6e97d8406d6d23e21b4b850` | Publish step changed to default local ticket set + dependency graph; real-tracker publication (never dozens automatically) gated on an explicit contract. | Same rationale as to-spec — ticket drafting shouldn't require tracker-write authority. | `to-tickets-local-first` |
| to-tickets (frontmatter) | `skills/engineering/to-tickets/SKILL.md` | `4a21285cbd01c7d2c6e97d8406d6d23e21b4b850` | `description` field reworded from "published to the configured tracker" to "written as a local ticket set by default — publishing ... only under an explicit task contract". | Same rationale as to-spec's frontmatter fix. | `to-tickets-local-first` |
| wayfinder | `skills/engineering/wayfinder/SKILL.md` | `7fe64c1057c2810c72941c07c8c7ad21aae42c45` | Map/tickets default to local files (`.scratch/` or `/tmp`); any tracker mutation requires an explicit contract. Reapplied via 3-way merge in the 068b6e0c refresh: upstream added a new "the issue tracker should have been provided to you" sentence right after our patch block, which now follows it unchanged. | Wayfinder's map/ticket creation are tracker mutations that need the same gating as everything else. | `wayfinder-local-default` |
| wayfinder (step 5) | `skills/engineering/wayfinder/SKILL.md` | `7fe64c1057c2810c72941c07c8c7ad21aae42c45` | Research subagents scoped to the active session and its agent cap; no `research/<name>` branch push without an authorizing envelope. Reapplied via 3-way merge in the 068b6e0c refresh: upstream changed "spin up a `/research` subagent" to "spin up a subagent that calls the Skill tool with 'research'" (an invocation-mechanism wording update, not a capability change) — adopted into the patch note; upstream's own `research/<name>` branch-push text is still what our patch overrides. | Same rationale as the row above, applied to the research-subagent-fanout step specifically. | `wayfinder-local-default` |
| improve-codebase-architecture | `skills/engineering/improve-codebase-architecture/SKILL.md` | `86c025ba94c45a4a85bdbf2e0318b3b72044d608` | Report generation changed from Tailwind/Mermaid CDN imports to fully self-contained HTML (inline CSS, inline SVG/ASCII diagrams); auto-open (`xdg-open`/`open`/`start`) removed in favor of printing the path. Same two changes mirrored in `HTML-REPORT.md`'s scaffold and diagram-pattern sections. | A CDN-loading HTML report is a network dependency and a mild supply-chain surface; auto-opening a file is an unrequested side effect. | `architecture-selfcontained-html`, `architecture-no-autoopen` |
| handoff | `skills/productivity/handoff/SKILL.md` | `2eb98a51b97bb5bac461a26ad14828eeac827909` | Save location changed from the OS temp directory to a gitignored `.scratch/` directory or the session scratchpad; added exact repo/branch/HEAD SHA + contract state + next step; strengthened redaction wording (API keys, OAuth tokens, cookies, passwords -> `[REDACTED]`, omit if unsure). | A world-readable OS temp handoff file is a weaker boundary than a gitignored in-workspace location; handoffs need enough state to resume safely without re-deriving it. | `handoff-scratch-redact` |
| teach | `skills/productivity/teach/SKILL.md` | `b1603e5ac536d2b5c29496c06df4db0bb9f74e69` | Teaching workspace changed from "the current directory" to a dedicated learning directory the user confirms first, never the production source repo root; removed auto-opening lesson files in favor of printing the path. | Prevents a teaching session from treating this Go repo's working tree as scratch space for lesson HTML/asset files. | `teach-workspace` |
| diagnosing-bugs | `skills/engineering/diagnosing-bugs/SKILL.md` | `ad911b016e5ebddb305daf9b02f6258e38c45617` | Post-mortem checklist item "stated in the commit / PR message" changed to "recorded in the diagnosis summary and, when the task contract authorizes a commit, in the commit/PR message too." Reapplied via 3-way merge in the 068b6e0c refresh: upstream deleted the following "Then ask: what would have prevented this bug?" paragraph and renamed the section header from "Phase 6 — Cleanup + post-mortem" to "Phase 6 — Cleanup"; the patched bullet text itself is unchanged and now ends the section. | The finding must be captured even when no commit is authorized for the session. | `diagnosing-bugs-commit-note` |
| ask-matt | `skills/engineering/ask-matt/SKILL.md` | `7f3ab7829fba2321ac9e5b2ff9d58b8321734aa8` | "Precondition" section's pointer to `/setup-matt-pocock-skills` replaced with a pointer to this project's already-integrated config (`docs/agents/issue-tracker.md`, `domain.md`, `triage-labels.md`); explicit note not to suggest running the excluded setup skill. Extended in the 068b6e0c refresh to also cover `/to-questionnaire` (newly promoted upstream, excluded by owner decision — see manifest `excluded[]`), inline where the router lists it as a bullet, plus the precondition-block summary. | `setup-matt-pocock-skills` and `to-questionnaire` are excluded from this project's vendored set (see manifest); ask-matt must not route users to a skill that isn't installed. | `ask-matt-setup-pointer` |
| codebase-design | `skills/engineering/codebase-design/DESIGN-IT-TWICE.md` | `8419ad6fa966be2e9e435c932b3584df56cb1af6`* | "Spawn 3+ sub-agents in parallel" changed to respect the active task contract's `agent_cap`, reducing the count below 3 if the cap requires it. Reapplied via 3-way merge in the 068b6e0c refresh: upstream simplified its own lead sentence (dropped "using the Agent tool"), which the patch now sits inside unchanged. | Prevents Design-It-Twice from exceeding the session's agent budget set by the task contract. | `design-it-twice-cap` |
| ask-matt | `skills/engineering/ask-matt/SKILL.md` | `7f3ab7829fba2321ac9e5b2ff9d58b8321734aa8` | Corrected the `/wizard` bullet's "Model-invoked, so the agent reaches for it the moment it hits a wall" claim, since this vendored copy patches `wizard` to explicit-invocation-only. | A router describing a skill's invocation mode must match how this project actually vendored it, not upstream's default. | `wizard-user-invoked` |
| ask-matt | `skills/engineering/ask-matt/SKILL.md` | `7f3ab7829fba2321ac9e5b2ff9d58b8321734aa8` | Reworded the `/wait-what` bullet's "using the `CONTEXT.md` vocabulary" to "this project's domain/spec vocabulary (`CONTEXT.md`, `SPECIFICATIONS.md`)". | Mirrors the same patch applied to `wait-what/SKILL.md` itself — see that row for the rationale. | `wait-what-domain-vocab` |
| wizard (new skill, added 068b6e0c refresh) | `skills/engineering/wizard/SKILL.md` | `fdc2a72162b75db138df2f81c9cc6fd8a72f4495` | Added `disable-model-invocation: true`; inserted a governance preamble after the title: explicit-invocation-only; authoring a wizard still requires the active task contract to authorize the specific target operation (infra provisioning, credentials/CI secrets, third-party-dashboard setup, migration/cutover); the agent never runs the generated script itself, not even to test one stage (upstream's own "don't run it end-to-end" step-4 advice, made a hard rule); the generated script is never committed/pushed without a separate explicit envelope; a production/TrueNAS/remote-host/real-secret target must be flagged to the user before authoring anything; secret values encountered while scoping/authoring are always `[REDACTED]` in the agent's own output, never echoed verbatim; this project's `CLAUDE.md`/`.claude/rules/**`/hooks always override the skill. `template.sh` itself is left unpatched (byte-identical to upstream) — it is only ever run by the human, so the governance boundary lives entirely in `SKILL.md`. | Upstream is model-invoked by default and assumes the agent may author (and the human may then run) scripts that write `.env`, set GitHub secrets/variables, and drive migrations/cutovers, with no tie-back to this project's contract-gated mutation model; a skill this consequential should never fire from model judgment alone. | `wizard-governance-gate` |
| wait-what (new skill, added 068b6e0c refresh) | `skills/productivity/wait-what/SKILL.md` | `d493cfa883645bb7ce2e12f5232c0fbe3a0b9f87` | Reworded "use the ubiquitous language from `CONTEXT.md`" to "use this project's domain/spec vocabulary — `CONTEXT.md` and `SPECIFICATIONS.md` at the repo root (see `docs/agents/domain.md` for how they relate)". | Upstream's wording assumes `CONTEXT.md` alone is the vocabulary source; per `docs/agents/domain.md`, this project's authoritative domain/spec terminology often lives in `SPECIFICATIONS.md` (and `CLAUDE.md`) first, with `CONTEXT.md` as a supplementary minimal skeleton — the smallest wording fix that matches this repo's actual layout instead of overstating `CONTEXT.md`'s role. Already user-invoked upstream (`disable-model-invocation: true`); kept as-is, no other change. Read-only, grants no new capability. | `wait-what-domain-vocab` |
| writing-for-agents (replaces writing-great-skills, 068b6e0c refresh) | `skills/productivity/writing-for-agents/SKILL.md` | `c059d48cd70cfd1494bde74afcc426010b1c10e3` | Added `disable-model-invocation: true`; inserted a short governance note after the frontmatter: explicit-invocation-only, reference material only, grants no standing authority to edit this project's `CLAUDE.md`/`AGENTS.md`/`.claude/rules/**`/hooks/settings/any tracked file without the active task contract authorizing it. `SKILL-MECHANICS.md` (sibling reference file) is left unpatched (byte-identical to upstream) — it carries no invocation mode or capability claims of its own. | Upstream is model-invoked by default and its own description names "modifying AGENTS.md or CLAUDE.md" as a trigger; this project's `CLAUDE.md` is itself the governance document, so a reference skill about writing it should never fire autonomously mid-task. Mirrors the posture the replaced `writing-great-skills` already had (also user-invoked) rather than broadening it. | `writing-for-agents-user-invoked` |

\* `DESIGN-IT-TWICE.md`, `UI.md`, and `LOGIC.md` are sibling reference files, not `SKILL.md`. Their blob SHA
column records each file's *own* upstream blob SHA (per `scratchpad/upstream-blob-shas.txt` at review time),
which differs from the skill's `SKILL.md` SHA recorded in `mattpocock-skills-manifest.json` — that manifest
only tracks the `SKILL.md` blob per skill; this ledger is the place a sibling file's own SHA is recorded. See
`docs/agents/mattpocock-skills-manifest.json` for the full file set vendored per skill.
