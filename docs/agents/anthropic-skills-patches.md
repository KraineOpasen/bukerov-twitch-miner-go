# Anthropic skills — local patch ledger

Every local change applied to a vendored skill from [anthropics/skills](https://github.com/anthropics/skills),
one row per patch id per touched file. Each change is marked in the vendored file itself with
`<!-- bukerov-local-patch: <id> --> ... <!-- /bukerov-local-patch: <id> -->` (Markdown/HTML) or
`# bukerov-local-patch: <id> — <note>` (Python) comments around or adjacent to the touched text. All
unpatched content is byte-identical to upstream commit `b29e7cf65e5cb78a5ac33d582270551bc74a14eb` (see
`anthropic-skills-manifest.json` for the per-file `upstream_blob_sha`). No translations, no stylistic
rewrites — every change here is a capability/safety constraint, not a wording preference. This is a
modification notice under Apache License 2.0 §4(b) ("You must cause any modified files to carry
prominent notices stating that You changed the files").

| Skill | Upstream path | Blob SHA | Local change | Reason | Rule |
| --- | --- | --- | --- | --- | --- |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | Frontmatter `name: skill-creator` → `name: skill-creator-anthropic`. | Upstream's name collides with the Claude Code built-in `skill-creator` skill; a distinct name lets both coexist without one shadowing the other. | `skc-rename-vendored` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | Added frontmatter `disable-model-invocation: true`, moving invocation from model-triggered to explicit `/skill-creator-anthropic` only. | A skill that authors and packages other skills (subagent fan-out, `claude -p` subprocesses, description-optimization loops) should never fire implicitly from model judgment. | `skc-user-invoked` |
| skill-creator-anthropic | `skills/skill-creator/scripts/quick_validate.py` | `ed8e1dddce77b16af13c6f36b3fe86c4ac7c590c` | Added `'disable-model-invocation'` to `ALLOWED_PROPERTIES`. | Direct, minimal consequence of the `skc-user-invoked` frontmatter change above: this vendored copy's own `SKILL.md` now sets `disable-model-invocation`, and `quick_validate.py`'s allowlist (which predates that Claude Code frontmatter key) would otherwise flag this skill's own frontmatter as invalid. Discovered by running `quick_validate.py` against this skill as part of this review's self-checks. | `skc-user-invoked` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | Inserted a ~15-line governance preamble immediately after the frontmatter (before "# Skill Creator"): explicit-invocation-only note; all workspaces/evals/reports/`.skill` files go under `/tmp` or `.scratch/`, never tracked `.claude/skills/**`/`.claude/commands/**` outside CHANGE mode; no background execution or auto-open; full eval/benchmark loop only on explicit request; `feedback.json`/eval outputs are untrusted data; concurrent fan-out capped by the task contract's `agent_cap`. | Upstream assumes standing write access to the repo's own skill tree, background execution, and unlimited subagent fan-out — none of which this project grants by default. | `skc-change-mode-gate` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | "Put results in `<skill-name>-workspace/` as a sibling to the skill directory" → `/tmp/<skill-name>-workspace/` (or `.scratch/`); same id also wraps the `evals/evals.json` save-location sentence to route it into the `/tmp` workspace when the skill under work is an installed/tracked skill. | A sibling workspace directory next to a tracked skill would itself become a tracked (or at least present-in-repo) artifact; eval scratch data shouldn't land in the repo either. | `skc-workspace-tmp` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | Appended a sentence after "Launch everything at once so it all finishes around the same time": cap applies from the governance preamble; batch serially, keeping with-skill/baseline pairs together, when `agent_cap` < 2×(test cases). | Prevents the "spawn all runs in the same turn" instruction from exceeding the session's agent budget. | `skc-agent-cap` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | Replaced the `nohup ... &` / `VIEWER_PID=$!` background-launch block with a foreground `--static <path>` invocation; shrank the Cowork/headless paragraph to "static mode is the only supported mode in this vendored copy"; changed the "Tell the user" quote from "I've opened the results in your browser" to "the results page is at \<path\> — open it in your browser"; deleted the "Kill the viewer server" `kill $VIEWER_PID` block (replaced with a short note that there's no server process to kill); updated the matching Cowork-section bullet for consistency. | `nohup ... &` is background execution the governance preamble forbids; a backgrounded HTTP server left running (with a PID the agent has to remember to kill) is unnecessary once the tool always writes a static file. | `skc-no-nohup-static-viewer` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | "Write to a temp file ... and open it: `open /tmp/eval_review_<skill-name>.html`" → print the path for the user to open themselves; no auto-open. | `open` is an unrequested side effect (and doesn't exist / does the wrong thing outside macOS); printing the path is strictly more portable. | `skc-no-open-evalreview` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | Step 3 ("Run the optimization loop") rewritten: drop "in the background and check on it periodically"; run from a `/tmp` sandbox directory that has its own `.claude/commands/` tree; run the loop as `PYTHONPATH=<skill-creator-path> python -m scripts.run_loop ...` (with a prose sentence explaining `PYTHONPATH` must point at the installed skill directory so `scripts.*` imports resolve while cwd stays in the sandbox); add `--results-dir <workspace>/description-opt`; note `--num-workers` must respect the task contract's concurrency cap; "it opens an HTML report in the browser" → "it writes an HTML report and prints its path". | `run_eval.py` (invoked by `run_loop.py`) writes transient command files under a resolved project's `.claude/commands/`; running it from a `/tmp` sandbox (paired with the `skc-py-sandbox-guard` script-level guard) keeps those files out of this tracked repo regardless of cwd when the loop is launched. Without `PYTHONPATH`, the documented command dead-ends both ways: from the `/tmp` sandbox, `scripts.run_loop`'s own `from scripts.run_eval import ...` raises `ModuleNotFoundError` (verified); from the skill directory, `find_project_root()` walks up to the repo's `.git` and the `skc-py-sandbox-guard` correctly refuses with exit 2. `PYTHONPATH` decouples "where imports resolve from" from "what cwd the sandbox guard sees," letting both constraints hold at once. | `skc-runloop-foreground-sandbox` |
| skill-creator-anthropic | `skills/skill-creator/SKILL.md` | `65b3a402dbd09b8e83f9d637c6b553875189085c` | Packaging step: pass an explicit output directory under the `/tmp` workspace (`package_skill.py`'s optional second argument) instead of relying on the cwd default. | The cwd default could otherwise drop the produced `.skill` zip into the tracked repository. | `skc-package-output-tmp` |
| skill-creator-anthropic | `skills/skill-creator/eval-viewer/viewer.html` | `6d8e96348a02e66c3363d2ff3b3ae58ac11e6382` | Removed the Google Fonts preconnect/stylesheet `<link>`s and the `cdn.sheetjs.com` `<script>` tag; changed `'Poppins', sans-serif` → `system-ui, -apple-system, 'Segoe UI', sans-serif` and `'Lora', Georgia, serif` → `Georgia, 'Times New Roman', serif` throughout; guarded `renderXlsx()` with `typeof XLSX === 'undefined'` → render "xlsx preview unavailable in offline mode — download the file instead" instead of calling the now-absent global. | This page is generated and opened locally; loading fonts and a spreadsheet library from two external CDNs on every view is an unnecessary, unreviewable network dependency for a file that should be fully self-contained. | `skc-html-no-cdn` |
| skill-creator-anthropic | `skills/skill-creator/assets/eval_review.html` | `938ff32aed9bffabf723bd5492d720f4736c8e4d` | Removed the Google Fonts preconnect/stylesheet `<link>`s; same Poppins/Lora → system-font-stack substitution as `viewer.html` (no SheetJS usage in this file, so no XLSX guard needed). | Same rationale as `viewer.html` — this HTML template is also generated and opened locally. | `skc-html-no-cdn` |
| skill-creator-anthropic | `skills/skill-creator/scripts/generate_report.py` | `959e30a0014ec165c41a2bb7420b7dfe1416bbac` | Removed the three Google Fonts `<link>` lines from the embedded HTML template string; same Poppins/Lora → system-font-stack substitution. | Same rationale — the HTML this script generates is opened locally, not served, so it should carry no external dependency. | `skc-html-no-cdn` |
| skill-creator-anthropic | `skills/skill-creator/eval-viewer/generate_review.py` | `7fa5978631fed1ed545591dbb2b0eb21ce3f3d08` | Removed `import webbrowser`; after arg parsing, added `if not args.static: sys.exit(2)` with a message pointing at `--static <output.html>`; removed the `webbrowser.open(url)` call in the (now unreachable) server path, replaced with a comment. Server-mode code otherwise left untouched. | The server mode opens an unauthenticated `localhost` HTTP endpoint and auto-opens a browser; this vendored copy only supports the already-static-only workflow the SKILL.md now documents (`skc-no-nohup-static-viewer`). | `skc-py-static-only-viewer` |
| skill-creator-anthropic | `skills/skill-creator/scripts/run_eval.py` | `e58c70bea39d5b252a1e819f242bbdcdf20e8b87` | `env = {k: v for k, v in os.environ.items() if k != "CLAUDECODE"}` → `env = dict(os.environ)`. | The `CLAUDECODE` guard is intentionally preserved per Bukerov governance: if `claude -p` refuses to nest inside an existing Claude Code session, that failure must be visible, not silently bypassed by stripping the variable. | `skc-py-no-claudecode-strip` |
| skill-creator-anthropic | `skills/skill-creator/scripts/improve_description.py` | `06bcec76122446986e3610c20a39c466de36f495` | Same `env = {...if k != "CLAUDECODE"}` → `env = dict(os.environ)` substitution as `run_eval.py`. | Same rationale as `run_eval.py` — this script calls `claude -p` via the identical subprocess pattern. | `skc-py-no-claudecode-strip` |
| skill-creator-anthropic | `skills/skill-creator/scripts/run_eval.py` | `e58c70bea39d5b252a1e819f242bbdcdf20e8b87` | In `run_single_query()`, before creating `.claude/commands/<name>.md`: added a fail-closed guard — if the resolved `project_root` contains a `.git` entry, print a message and `sys.exit(2)` instead of writing. | `find_project_root()` walks up from cwd looking for a `.claude/` directory; run from the repo root (or any subdirectory) it would otherwise write transient command files straight into this tracked repository. Placed in `run_single_query()` (not only in `main()`) because `run_loop.py` calls `run_eval()` as a library function, bypassing `run_eval.py`'s own `main()`. | `skc-py-sandbox-guard` |
| skill-creator-anthropic | `skills/skill-creator/scripts/run_loop.py` | `30a263d674ef19de11c756d6f7537f91a421909e` | Removed `import webbrowser`; replaced `webbrowser.open(str(live_report_path))` with `print(f"Live report: {live_report_path}", file=sys.stderr)`. | The optimization loop's live report should never auto-open a browser; the caller (SKILL.md, per `skc-runloop-foreground-sandbox`) now always prints and reports the path itself. | `skc-py-no-autoopen-runloop` |
| frontend-design | `skills/frontend-design/SKILL.md` | `decdff43d05908b4c1fc2cfd2d80fc5743440934` | Frontmatter `description` appended: "In this repository: visual/UI work on the existing Go html/template dashboard only — not backend/API/schema work, not multi-variant exploration (use the prototype skill), not chart-color conventions (dataviz owns those)." | Narrows the trigger surface so this skill doesn't compete with `codebase-design`, `prototype`, or `dataviz` on adjacent-but-different requests. | `fd-description-scope` |
| frontend-design | `skills/frontend-design/SKILL.md` | `decdff43d05908b4c1fc2cfd2d80fc5743440934` | Inserted a stack-pin paragraph after the opening paragraph: Go `html/template` + Tailwind + HTMX + ApexCharts, no frameworks/new build steps, no CDN assets/external fonts (dashboard is `go:embed`-only), backend/API/schema out of scope, one already-chosen direction per invocation (multi-variant exploration belongs to `prototype`). | Upstream is stack-agnostic by design; this repository has one fixed frontend stack, and a design skill let loose on it without that constraint could suggest a framework migration or a CDN asset. | `fd-stack-pin` |
| frontend-design | `skills/frontend-design/SKILL.md` | `decdff43d05908b4c1fc2cfd2d80fc5743440934` | Appended a paragraph at the end of "Restraint and self-critique": screenshots/scratch notes go only under `/tmp` or `.scratch/`, taken against a locally running dashboard (never production/TrueNAS, never real credentials); tracked template/CSS edits only in CHANGE mode on the contract's branch. | Ties the skill's own "take screenshots as you critique" advice back to this project's artifact-location and mode rules instead of leaving it unstated. | `fd-artifact-paths` |
| webapp-testing | `skills/webapp-testing/SKILL.md` | `4726215301db64a0cc4d41fc3219c61f37a30f4a` | Frontmatter `description` appended: "Local targets only. For bug diagnosis the diagnosing-bugs skill owns the loop — this skill supplies browser evidence. Not a replacement for go test." | Narrows the trigger surface relative to `diagnosing-bugs`, `tdd`/`go test`, and prevents this skill from being asked to hit a remote target. | `webapp-testing-description-scope` |
| webapp-testing | `skills/webapp-testing/SKILL.md` | `4726215301db64a0cc4d41fc3219c61f37a30f4a` | "Always run scripts with `--help` first ... DO NOT read the source until you try running the script first" → "Read and audit every bundled script before its first execution in this session ... After auditing, run with `--help` to confirm usage. Never execute bundled or generated scripts blind." | Running an unread script (even a bundled one) is a blind-execution risk; auditing first costs little for a ~150-line script and is this project's baseline expectation for any script it's about to run. | `webapp-testing-audit-before-exec` |
| webapp-testing | `skills/webapp-testing/SKILL.md` | `4726215301db64a0cc4d41fc3219c61f37a30f4a` | Best Practices bullet "Use bundled scripts as black boxes" → "Use bundled scripts only after auditing them — read the script, then use `--help`, then invoke." | Same rationale as `webapp-testing-audit-before-exec`, applied to the Best Practices bullet that repeated the black-box framing. | `webapp-testing-no-blackbox` |
| webapp-testing | `skills/webapp-testing/SKILL.md` | `4726215301db64a0cc4d41fc3219c61f37a30f4a` | Appended six bullets to the end of Best Practices: localhost/127.0.0.1/file:// targets only (never production/staging/remote/TrueNAS); credentials only from env vars, redacted as `[REDACTED]` in reports; artifacts only under `/tmp`/`.scratch/`; never `playwright install` (Chromium is pre-installed); never start the app via Docker (deny-listed) — use `go run ./cmd/miner` or the built binary; bug diagnosis stays owned by `diagnosing-bugs`. | Upstream has no target/credential/artifact-location constraints at all — these are this project's non-negotiables for any tool that drives a real browser against a real port. | `webapp-testing-localhost-only` |
| webapp-testing | `skills/webapp-testing/SKILL.md` | `4726215301db64a0cc4d41fc3219c61f37a30f4a` | Multi-server example rewritten from `--server "cd backend && python server.py" --port 3000 --server "cd frontend && npm run dev" --port 5173` to the new `--server "python server.py" --cwd backend --port 3000 --server "npm run dev" --cwd frontend --port 5173` form, with a short note that `with_server.py` rejects shell metacharacters and runs commands with `shell=False`. | The old example's own `cd x && ...` idiom is exactly the shell-chaining pattern the hardened script now refuses to run; the example had to change in lockstep with the script. | `webapp-testing-server-cleanup` |
| webapp-testing | `skills/webapp-testing/scripts/with_server.py` | `431f2eba16b268b7f3e2ae4daae9db41c0289b6d` | Full hardening rewrite: `shell=True` → `shell=False` with each `--server` value `shlex.split` and checked against a shell-metacharacter denylist (`&& \|\| ; \| & > <` backtick `$(`) before anything runs (exit 2, nothing executed, on a hit); new repeatable `--cwd` option (count must match `--server`) replaces the `cd x && ...` idiom; each server now runs via `subprocess.Popen(argv, cwd=..., start_new_session=True, stdout=logfile, stderr=subprocess.STDOUT)` with `logfile` at `/tmp/webapp-testing/server-<idx>-port<port>.log` (path printed); cleanup now targets the whole process group (`os.killpg(getpgid(pid), SIGTERM)`, wait up to 5s, then `SIGKILL`, with `ProcessLookupError` handled at each step) instead of `process.terminate()` on just the immediate child. Module docstring examples updated to the `--cwd` form. | Upstream's `shell=True` ran arbitrary shell strings verbatim (including operator-supplied `--server` values) and only ever terminated the immediate child, silently orphaning any grandchild process a dev server spawns (common for `npm run dev` wrapping a real process). Both are exactly the failure modes this project's governance treats as unacceptable in a bundled script that runs by default. | `webapp-testing-server-cleanup` |
| webapp-testing | `skills/webapp-testing/examples/console_logging.py` | `9329b5e232ed53d944bfe95e750070117d31b9ab` | `/mnt/user-data/outputs/console.log` → `/tmp/webapp-testing/console.log`; added `import os` + `os.makedirs('/tmp/webapp-testing', exist_ok=True)`. | `/mnt/user-data/outputs/` doesn't exist in this environment (that path is a Claude.ai-specific convention); `/tmp` is this project's standard scratch location. | `webapp-testing-artifact-paths` |
| webapp-testing | `skills/webapp-testing/examples/static_html_automation.py` | `90bbedcc08cdb345c74fb938ebad02c0baf56603` | `/mnt/user-data/outputs/static_page.png` and `/mnt/user-data/outputs/after_submit.png` → `/tmp/webapp-testing/...`; added `os.makedirs('/tmp/webapp-testing', exist_ok=True)` (`import os` was already present). | Same rationale as `console_logging.py`. `examples/element_discovery.py` already used `/tmp` and needed no change. | `webapp-testing-artifact-paths` |
| webapp-testing (new file) | n/a — local origin, no upstream path | n/a | Added `.claude/skills/webapp-testing/scripts/test_with_server.py`: stdlib-only regression tests for the `webapp-testing-server-cleanup` rewrite (noisy-stdout server doesn't deadlock and its log is captured; a server's grandchild process is reaped via the process-group kill; a `--server` value with a shell metacharacter is rejected with exit 2 and nothing runs). Runtime well under 40s, no network beyond loopback. | A hardening rewrite this size (shell handling, process groups, cleanup ordering) needs its own regression coverage; upstream ships no tests for this script. | `webapp-testing-server-cleanup` |
